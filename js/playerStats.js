export class PlayerStats {
  constructor(strBonus, meleeAccBonus, magicStrength, magicAccBonus, rangedAccBonus, wearingVoid) {
    this.str_bonus = strBonus;
    this.melee_acc_bonus = meleeAccBonus;
    this.magic_strength = magicStrength;
    this.magic_acc_bonus = magicAccBonus;
    this.ranged_acc_bonus = rangedAccBonus;
    this.wearing_void = wearingVoid;
  }
}